Files already downloaded and verified
Files already downloaded and verified
TwoLayerNet(
  (sigmoid): Sigmoid()
  (softmax): Softmax(dim=None)
)
Files already downloaded and verified
Files already downloaded and verified
TwoLayerNet(
  (sigmoid): Sigmoid()
  (softmax): Softmax(dim=None)
)
